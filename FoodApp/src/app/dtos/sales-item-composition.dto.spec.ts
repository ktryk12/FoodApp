import { SalesItemComposition } from './sales-item-composition.dto';

describe('SalesItemComposition', () => {
  it('should create an instance', () => {
    expect(new SalesItemComposition()).toBeTruthy();
  });
});
