import { IngredientOrderline } from './ingredient-orderline.dto';

describe('IngredientOrderline', () => {
  it('should create an instance', () => {
    expect(new IngredientOrderline()).toBeTruthy();
  });
});
